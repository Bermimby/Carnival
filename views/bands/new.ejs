<%- include('../partials/header') %>


<section id="badge-container">
 
  <h2>Enter a  Review</h2>

<form action="/bands/<%=band._id%>" method="POST">


      </label><br>
 
  <label>Review:
    <input type="string" name="content">
  </label><br>
<button type="submit">Add Review</button>

<%band.reviews.forEach(b=>{ %>
<span><%b.content%></span>
<% }) %>
</form>

<h3><%= band.name %></h3> 
<% band.reviews.forEach(function(r){%>
  <form action="/bands/<%=band._id %>/<%= r._id %>?_method=DELETE"
    class="delete-form" method="POST">
    <button type="submit">X</button>
  </form>
  <a href="/bands/<%= band.id %>"></a>

 
 <ol>

 <li><%= r.content %></li>
 </ol>

  <% }) %>
</section>

<%- include('../partials/footer') %> 
